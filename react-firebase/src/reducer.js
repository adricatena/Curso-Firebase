const reducer = (state, action) => {
  /* if (action.type === "ADD_PACK") {
     const tempState = action.payload;
    return {...state,  carrito.packs: tempState };
  } */
  throw new Error("'action.type' no encontrada");
};
export default reducer;
